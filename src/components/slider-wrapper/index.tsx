// Styles
import { FlickityWrapper } from './style';

const SliderWrapper: React.FC = ({ children }) => {
	return (
		<FlickityWrapper>
			{children}
		</FlickityWrapper>
	);
};

export default SliderWrapper;