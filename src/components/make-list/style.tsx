// Packages
import styled, { css } from 'styled-components';

const MakeListWrapper = styled.div`
	display: block;
`;
const MakeListContainer = styled.ul`
	display: block;
`;
const MakeListInfo = styled.li`
	display: block;
`;
const MakeListText = styled.a`
	display: block;
`;
const MakeListSpan = styled.span`
	display: block;
`;

export {
	MakeListWrapper,
	MakeListContainer,
	MakeListInfo,
	MakeListText,
	MakeListSpan
};